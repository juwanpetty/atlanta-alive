<section class="all-feed">
  <ul class="all-feed__list">
    <% @photos.each do |photo| %>
      <li class="all-feed__list__item">
        <%= link_to image_tag(photo.image.url()), photo_path(photo) %>
      </li>
    <% end %>
  </ul>
</section>